from setuptools import setup, find_packages

setup(
    name='adaptive_filters',
    packages=find_packages(include=['adaptive_filters']),
    version='0.1.0',
    description='A simple signal processing library for adaptive filters.',
    author='Michel Hardenberg',
    license='MIT',
)
